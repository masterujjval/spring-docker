## Springboot app custom docker container

This simple springboot app is containerised using docker

## Steps to run the container
- git clone the repo
- run the docker compose file
- ```docker compose up --build ```
